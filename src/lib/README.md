# src/lib/

Shared client/runtime library code. Populated in later sprints:
`supabase.ts` (client), `hours.ts` (client mirror of `is_open_now`),
`schema.ts` (types generated from the Supabase schema). Empty scaffold for now.

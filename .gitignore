# build output
dist/
# astro generated types
.astro/
# deps
node_modules/
# env
.env
.env.production
# test artifacts (visual-regression diffs/reports; committed *-snapshots/ baselines are kept)
test-results/
playwright-report/
# internal operational reports (contain member UUIDs — never commit)
reports/
# partner-report CSV importer drop-zone (raw guest data / PII) — never commit.
# reports/ above already covers this; listed explicitly so the inbox intent is clear.
reports/inbox/
# os
.DS_Store

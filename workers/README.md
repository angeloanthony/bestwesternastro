# workers/

Cloudflare Workers for server-side secrets / non-trivial logic:
`ask.ts` (AI Concierge — ADR-005), `plan.ts` (trip planner rules engine),
`lead-notify.ts` (front-desk lead alerts). Empty scaffold for now — no Wrangler config in this PR.
